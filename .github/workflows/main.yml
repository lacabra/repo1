name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - id: file_changes
        uses: trilom/file-changes-action@v1.2.3
      - name: test
        run:
          cat $HOME/files.json